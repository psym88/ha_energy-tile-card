{
  "name": "ha-energy-tile-card",
  "version": "1.0.3",
  "private": true,
  "description": "Energy consumption tiles for Home Assistant",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "npm run build && node --check dist/ha_energy-tile-card.js"
  }
}
